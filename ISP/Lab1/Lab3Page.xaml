<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab1.Lab3Page"
             Title="lab3page"
             Loaded="OnPageLoaded">
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            x:Name="MainStackLayout">
            <Label x:Name="header" Text="Choose Artist" FontSize="18" />
            <Picker x:Name="DBPicker" Title = "Artist and Music"
                SelectedIndexChanged="PickerSelectedIndexChanged">
                <Picker.Items>
                </Picker.Items>
            </Picker>
            <VerticalStackLayout x:Name= "ContentStackLayout">

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>